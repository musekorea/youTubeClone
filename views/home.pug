extends layout/main
include mixins/videoBlock

block content
    
  .videos 
    each video in videos 
      +videoBlock({
        fileUrl: video.fileUrl,
        id:video.id,
        title:video.title,
        views:video.views,
        })


